import './style.css';

import { initScrollMagic } from './drawLoop';

// import imgs from './data/imgs';
// import imgs1 from './data/imgs1';


(window as any).initScrollMagic = initScrollMagic;

// (window as any).imgs = imgs;

// (window as any).imgs1 = imgs1;
